let baseRange,isIterateeCall,toFinite;_3f2‍.x([["default",()=>_3f2‍.o]]);_3f2‍.w("./_baseRange.js",[["default",["baseRange"],function(v){baseRange=v}]]);_3f2‍.w("./_isIterateeCall.js",[["default",["isIterateeCall"],function(v){isIterateeCall=v}]]);_3f2‍.w("./toFinite.js",[["default",["toFinite"],function(v){toFinite=v}]]);



/**
 * Creates a `_.range` or `_.rangeRight` function.
 *
 * @private
 * @param {boolean} [fromRight] Specify iterating from right to left.
 * @returns {Function} Returns the new range function.
 */
function createRange(fromRight) {
  return function(start, end, step) {
    if (step && typeof step != 'number' && isIterateeCall(start, end, step)) {
      end = step = undefined;
    }
    // Ensure the sign of `-0` is preserved.
    start = toFinite(start);
    if (end === undefined) {
      end = start;
      start = 0;
    } else {
      end = toFinite(end);
    }
    step = step === undefined ? (start < end ? 1 : -1) : toFinite(step);
    return baseRange(start, end, step, fromRight);
  };
}

_3f2‍.d(createRange);
