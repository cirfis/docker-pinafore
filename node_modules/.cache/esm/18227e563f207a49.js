let baseKeys,getTag,isArrayLike,isString,stringSize;_3f2‍.x([["default",()=>_3f2‍.o]]);_3f2‍.w("./_baseKeys.js",[["default",["baseKeys"],function(v){baseKeys=v}]]);_3f2‍.w("./_getTag.js",[["default",["getTag"],function(v){getTag=v}]]);_3f2‍.w("./isArrayLike.js",[["default",["isArrayLike"],function(v){isArrayLike=v}]]);_3f2‍.w("./isString.js",[["default",["isString"],function(v){isString=v}]]);_3f2‍.w("./_stringSize.js",[["default",["stringSize"],function(v){stringSize=v}]]);





/** `Object#toString` result references. */
var mapTag = '[object Map]',
    setTag = '[object Set]';

/**
 * Gets the size of `collection` by returning its length for array-like
 * values or the number of own enumerable string keyed properties for objects.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Collection
 * @param {Array|Object|string} collection The collection to inspect.
 * @returns {number} Returns the collection size.
 * @example
 *
 * _.size([1, 2, 3]);
 * // => 3
 *
 * _.size({ 'a': 1, 'b': 2 });
 * // => 2
 *
 * _.size('pebbles');
 * // => 7
 */
function size(collection) {
  if (collection == null) {
    return 0;
  }
  if (isArrayLike(collection)) {
    return isString(collection) ? stringSize(collection) : collection.length;
  }
  var tag = getTag(collection);
  if (tag == mapTag || tag == setTag) {
    return collection.size;
  }
  return baseKeys(collection).length;
}

_3f2‍.d(size);
