let arrayFilter,arrayMap,baseProperty,baseTimes,isArrayLikeObject;_3f2‍.x([["default",()=>_3f2‍.o]]);_3f2‍.w("./_arrayFilter.js",[["default",["arrayFilter"],function(v){arrayFilter=v}]]);_3f2‍.w("./_arrayMap.js",[["default",["arrayMap"],function(v){arrayMap=v}]]);_3f2‍.w("./_baseProperty.js",[["default",["baseProperty"],function(v){baseProperty=v}]]);_3f2‍.w("./_baseTimes.js",[["default",["baseTimes"],function(v){baseTimes=v}]]);_3f2‍.w("./isArrayLikeObject.js",[["default",["isArrayLikeObject"],function(v){isArrayLikeObject=v}]]);





/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeMax = Math.max;

/**
 * This method is like `_.zip` except that it accepts an array of grouped
 * elements and creates an array regrouping the elements to their pre-zip
 * configuration.
 *
 * @static
 * @memberOf _
 * @since 1.2.0
 * @category Array
 * @param {Array} array The array of grouped elements to process.
 * @returns {Array} Returns the new array of regrouped elements.
 * @example
 *
 * var zipped = _.zip(['a', 'b'], [1, 2], [true, false]);
 * // => [['a', 1, true], ['b', 2, false]]
 *
 * _.unzip(zipped);
 * // => [['a', 'b'], [1, 2], [true, false]]
 */
function unzip(array) {
  if (!(array && array.length)) {
    return [];
  }
  var length = 0;
  array = arrayFilter(array, function(group) {
    if (isArrayLikeObject(group)) {
      length = nativeMax(group.length, length);
      return true;
    }
  });
  return baseTimes(length, function(index) {
    return arrayMap(array, baseProperty(index));
  });
}

_3f2‍.d(unzip);
