let LazyWrapper,arrayPush,arrayReduce;_3f2‍.x([["default",()=>_3f2‍.o]]);_3f2‍.w("./_LazyWrapper.js",[["default",["LazyWrapper"],function(v){LazyWrapper=v}]]);_3f2‍.w("./_arrayPush.js",[["default",["arrayPush"],function(v){arrayPush=v}]]);_3f2‍.w("./_arrayReduce.js",[["default",["arrayReduce"],function(v){arrayReduce=v}]]);



/**
 * The base implementation of `wrapperValue` which returns the result of
 * performing a sequence of actions on the unwrapped `value`, where each
 * successive action is supplied the return value of the previous.
 *
 * @private
 * @param {*} value The unwrapped value.
 * @param {Array} actions Actions to perform to resolve the unwrapped value.
 * @returns {*} Returns the resolved value.
 */
function baseWrapperValue(value, actions) {
  var result = value;
  if (result instanceof LazyWrapper) {
    result = result.value();
  }
  return arrayReduce(actions, function(result, action) {
    return action.func.apply(action.thisArg, arrayPush([result], action.args));
  }, result);
}

_3f2‍.d(baseWrapperValue);
